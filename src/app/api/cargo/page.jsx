'use server'

// import { Level } from 'level';

export const create = async (props) => {
   return (
      'here'
   )
   // const db = new Level('cargo',{ valueEncoding:'json'});
   // await db.put('0001', { weight: 3000});
}

export default create;